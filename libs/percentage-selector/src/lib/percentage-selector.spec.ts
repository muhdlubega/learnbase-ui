import { percentageSelector } from './percentage-selector';

describe('percentageSelector', () => {
  it('should work', () => {
    expect(percentageSelector()).toEqual('percentage-selector');
  });
});
